<?php

declare(strict_types=1);

namespace App\Constants;

final class Pagination
{
    /** Rows per page on every room-admin (/admin/{room}/...) list and its JSON index endpoint. */
    public const ADMIN_PER_PAGE = 15;
}
